# User Defined Command -> burst

Developed a command line application to split a file into multiple files with various command-line options, by implementing multithread and error handling concepts in C.
